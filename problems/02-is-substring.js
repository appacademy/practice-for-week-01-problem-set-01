/**
 * Write a function isSubstring(searchString, subString) that takes in two
 * strings, searchString and subString. The function should return true if
 * subString is a part of the searchString, regardless of upper or lower case,
 * and false if otherwise.
 *
 * HINT: try using the .indexOf() function on a string to see what it does!
 */

function isSubstring(searchString, subString) {
	// Your code here
}

/** Comment in these logs for local testing */
// console.log(isSubstring('The cat went to the store', 'he cat went')); // => true
// console.log(isSubstring('Time to program', 'time')); // => true
// console.log(isSubstring('Jump for joy', 'joys')); // => false

/**================== DO NOT MODIFY CODE BELOW THIS LINE ==================**/

module.exports = isSubstring;